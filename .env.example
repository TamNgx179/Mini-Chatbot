# Copy this file to .env, then replace both placeholder values.
# Never commit the real .env file.
OPENAI_API_KEY=replace_with_a_project_api_key

# Reuse the Vector Store that is attached to the OptiBot Assistant.
OPENAI_VECTOR_STORE_ID=vs_replace_with_your_vector_store_id
